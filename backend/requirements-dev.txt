# Dev/test dependencies — includes all runtime deps via -r requirements.txt
-r requirements.txt

# Testing
pytest>=9.0.3
pytest-cov==5.0.0
pytest-asyncio>=0.25.0
asgi-lifespan>=2.1.0

# Linting & type checking
ruff>=0.4.0
mypy>=1.10.0

# Security scanning
bandit>=1.8.3
pip-audit==2.7.3
